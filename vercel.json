{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "composer install --prefer-dist --no-dev --optimize-autoloader --no-interaction && php -r \"file_exists('.env') || copy('.env.example','.env');\" && php artisan key:generate --force && npm ci && npm run build",
    "outputDirectory": "public",
    "functions": {
        "api/index.php": {
            "runtime": "vercel-php@0.9.0"
        }
    },
    "routes": [
        {
            "src": "/build/(.*)",
            "dest": "/public/build/$1"
        },
        {
            "src": "/(.*)",
            "dest": "/api/index.php"
        }
    ]
}
